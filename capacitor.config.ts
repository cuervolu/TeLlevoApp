import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cuervolu.tellevoapp',
  appName: 'TeLlevoApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
